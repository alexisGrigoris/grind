/* 
DEFAULT
PARAMS
*/

const ftransfer = (player = "Someone",team = "this team") =>{
    console.log(`${player} is the new player of ${team}`);
}
ftransfer(); // if there are two parameters you cannot add a value only to the second parameter.
ftransfer("Noni Madueke","bvb");